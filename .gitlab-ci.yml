image: node:9.10.1

cache:
  paths:
  - node_modules/

tests:
  script:
  - npm install
  - npm run test

pages:
  script:
  - npm install
  - npm run build
  - mv dist public

  artifacts:
    paths:
    - public

  only:
    - master